import React from "react";
import ReactDOM from "react-dom";

const Modal = () => {
  return (
    <div>
      <button className="ui primary button">Modal Add</button>
    </div>
  );
};
export default Modal;
